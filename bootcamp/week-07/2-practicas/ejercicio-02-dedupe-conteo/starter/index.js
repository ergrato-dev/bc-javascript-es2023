export const countOccurrences = items => {
  // TODO: return a Map of item -> count
};

export const topNCounts = (map, n) => {
  // TODO: return array of [item, count] sorted desc by count, stable for ties
};

export const mergeCounts = (mapA, mapB) => {
  // TODO: return a new Map with summed counts from mapA and mapB
};
