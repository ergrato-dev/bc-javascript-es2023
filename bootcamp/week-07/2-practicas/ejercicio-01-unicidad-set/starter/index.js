export const uniqueIds = ids => {
  // TODO: return array with unique values preserving original order
};

export const mergeFeatures = (primary, secondary) => {
  // TODO: return a Set with union (primary first, then only new items from secondary)
};

export const hasAll = (required, availableSet) => {
  // TODO: return true if every required item exists in availableSet (which is a Set)
};
